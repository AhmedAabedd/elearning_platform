<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Inherit the course form view to add price field -->
    <record id="view_slide_channel_form_inherit" model="ir.ui.view">
        <field name="name">slide.channel.form.inherit</field>
        <field name="model">slide.channel</field>
        <field name="inherit_id" ref="website_slides.view_slide_channel_form"/>
        <field name="arch" type="xml">

            <xpath expr="//button[@name='action_redirect_to_members']" position="after">
                <button class="oe_stat_button" type="object" name="action_view_sales_order" icon="fa-money" invisible="enroll != 'invite'">
                    <field name="sales_total_formated" string="Sales" widget="statinfo"/>
                </button>
            </xpath>

            <xpath expr="//field[@name='website_id']" position="after">
                <field name="display_price_formated" string="Course Price" invisible="enroll != 'invite' or not product_template_id"/>
            </xpath>

            <xpath expr="//field[@name='enroll']" position="after">
                <field name="product_template_id" invisible="enroll != 'invite'" required="enroll == 'invite'"/>
            </xpath>
            
        </field>
    </record>

</odoo>